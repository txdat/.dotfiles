---
model: haiku
description: "Delegate code simplification to Codex. Use instead of /dev:simplify-code when you prefer Codex for simplification."
---

Delegate this entire task to Codex using the `codex:rescue` skill.
The target Codex skill is `dev-simplify-code` (reads `~/.ai-shared/skills/dev/simplify-code.md`).
Pass the full user request and working directory context to Codex.
