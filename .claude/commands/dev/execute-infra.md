---
model: sonnet
effort: medium
description: "Write infrastructure config (Terraform, k8s manifests) and a runbook without applying changes. Triggers: 'write the terraform', 'generate the k8s config', 'create the runbook'."
---

Read `~/.ai-shared/skills/dev/execute-infra.md` and follow all instructions exactly.
